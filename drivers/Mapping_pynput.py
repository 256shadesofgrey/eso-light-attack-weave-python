from bidict import bidict

from pynput.keyboard import Key
from pynput.mouse import Button

class Mapping:

    # Takes the library specific identification of the key/button and
    # returns the common identification string.
    def getCommon():
        return

    # Takes the common identification string of the key/button and
    # returns the library specific identification.
    def getLibSpecific():
        return
